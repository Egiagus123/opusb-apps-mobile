import 'package:flutter/material.dart';

import 'approval_body.dart';

class ApprovalScreen extends StatelessWidget {
  static String routeName = "/approval";
  @override
  Widget build(BuildContext context) {
    return ApprovalBody();
  }
}
